import Link from 'next/link'; import { Card } from '@/components/Card'; import { QuestHUD } from '@/components/QuestHUD'; import { quests } from '@/lib/mock-data';
export default function Home(){ const quest=quests[0]; return <main className="mx-auto min-h-screen max-w-md space-y-5 p-4 pb-24"><header className="pt-6"><div className="mb-5 flex justify-end"><a className="rounded-2xl bg-white px-4 py-2 text-sm font-black text-asphalt" href="/api/auth/signin">Sign in with Google</a></div><p className="text-sm font-semibold uppercase tracking-[0.3em] text-neon">IRL Quest Platform</p><h1 className="mt-2 text-4xl font-black leading-tight">Real-world multiplayer adventures.</h1><p className="mt-3 text-white/65">Create a crew, move through the city, complete short missions, and win by teamwork.</p></header><QuestHUD/><Card><h2 className="text-xl font-bold">{quest.title}</h2><p className="mt-1 text-sm text-white/60">{quest.city} • {quest.durationHours}h • {quest.teamSize} players • {quest.maxTeams} teams</p><p className="mt-3 text-white/75">{quest.description}</p><Link className="mt-5 block rounded-2xl bg-neon px-5 py-4 text-center font-black text-asphalt" href={`/quests/${quest.id}`}>Start quest</Link></Card><nav className="grid grid-cols-3 gap-3"><Link className="glass rounded-2xl p-4 text-center" href="/profile">Profile</Link><Link className="glass rounded-2xl p-4 text-center" href="/teams/t1">Team</Link><Link className="glass rounded-2xl p-4 text-center" href={`/quests/${quest.id}`}>Map</Link></nav></main>; }
