export async function GET(){ const stream = new ReadableStream({ start(controller){ const enc=new TextEncoder(); controller.enqueue(enc.encode(`data: ${JSON.stringify({type:'connected',message:'Quest realtime stream ready'})}\n\n`)); const timer=setInterval(()=>controller.enqueue(enc.encode(`data: ${JSON.stringify({type:'score',teamScore:910+Math.floor(Math.random()*60),missionProgress:'live'})}\n\n`)),2500); setTimeout(()=>{ clearInterval(timer); controller.close(); },15000); }}); return new Response(stream,{headers:{'Content-Type':'text/event-stream','Cache-Control':'no-cache','Connection':'keep-alive'}}); }
